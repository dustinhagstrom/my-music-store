import React from "react";
import Box from "@mui/material/Box";
import Layout from "../components/Layout";

const CartPage = () => {
  return (
    <Layout>
      <Box sx={{ height: "500px" }}>imagine this is the cart page</Box>
    </Layout>
  );
};

export default CartPage;
